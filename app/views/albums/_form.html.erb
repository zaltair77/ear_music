<%= form_for(album) do |f| %>
  <% if album.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(album.errors.count, "error") %> prohibited this album from being saved:</h2>

      <ul>
      <% album.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :artist %>
    <%= f.collection_select :artist, Artist.order(:name),:name,:name, include_blank: true  %>
  </div>

  <div class="field">
    <%= f.label :year %>
    <%= f.number_field :year %>
  </div>

  <div class="field">
    <%= f.label :tracks %>
    <%= f.number_field :tracks %>
  </div>

  <div class="field">
    <%= f.label :artwork %>
    <%= f.text_field :artwork %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
